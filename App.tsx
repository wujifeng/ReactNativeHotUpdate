import { Text, StyleSheet, View } from 'react-native'
import React, { Component } from 'react'

import Index from './src_01hot/index'

export default class App extends Component {
  render() {
    return (
      <View>
        <Index/>
      </View>
    )
  }
}

const styles = StyleSheet.create({})